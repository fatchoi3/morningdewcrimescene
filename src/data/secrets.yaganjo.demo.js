// ─────────────────────────────────────────────────────────────────────────────
// 야간조 데모 비밀팩 — **실제 정답을 담지 않는다.**
//
//   VITE_DEMO=1 빌드에서 src/data/secrets.yaganjo.js 대신 번들된다.
//   새벽이슬 src/data/secrets.demo.js 와 같은 자리이고 같은 이유다 — 공개 시연용
//   번들에 정답이 섞여 나가지 않게 한다.
//
//   값은 secrets.example.js 와 같다(네 자리는 전부 0000, 조회는 CHANGE-ME).
//   동작은 하되 게임은 성립하지 않는다 — 그것이 데모 빌드가 원하는 상태다.
// ─────────────────────────────────────────────────────────────────────────────
export default {
  // ── 감식 결과 열람 비번 아홉(L1~L9) ────────────────────────────────────────
  // mergeSecrets 가 clue.password 로 주입하고, localProvider.isGamsikProtected /
  // verifyGamsik 이 그 값을 본다. 데이터 쪽은 이미 준비돼 있어 값만 채우면 작동한다.
  // 진행자가 조마다 결과를 열어 주는 손잡이다(보드판의 「남이 읽는다」 자리).
  passwords: {
    'IJEO-08': '0000', // L1 · 텀블러 잔여물 감식      (검체 W1)
    'FBWD-37': '0000', // L2 · 약봉투 성분 감식        (검체 B1)
    'MJKI-42': '0000', // L3 · 파렛트 잭 손잡이 감식    (검체 X6)
    'VHVX-45': '0000', // L4 · 1단 랩 롤 감식          (검체 X7)
    'GAPY-25': '0000', // L5 · 3단 랩 덩어리 감식       (검체 X8)
    'ZUKG-03': '0000', // L6 · 시신 채취 감식          (검체 X9) — 이벤트 ④ 뒤에 준다
    'PKNM-32': '0000', // L7 · 작업화 감식             (검체 D2)
    'JRKW-82': '0000', // L8 · 지게차 조향대·레버 감식  (검체 N1)
    'PNOQ-92': '0000', // L9 · 보온병 감식             (검체 F4)
  },

  // ── 폰 화면 잠금 다섯 ──────────────────────────────────────────────────────
  // ★ **아직 아무도 읽지 않는 키다.** mergeSecrets 의 주입 경로는 passwords ·
  //   recover · lookups 셋뿐이고(src/data/mergeSecrets.js), PhoneModal 에도 잠금
  //   화면이 없다. 그래서 지금은 잠긴 폰 다섯이 앱에서 그냥 열린다.
  //   데이터 쪽 phone.lock({ digits, hint })은 이미 다섯 장에 들어 있으므로,
  //   mergeSecrets 에 분기 한 줄 + localProvider 에 verifyPhoneLock + PhoneModal 에
  //   잠금 화면(GamsikModal 의 입력 UI를 본뜬다)이 붙는 순간 그대로 작동한다.
  //   자리를 미리 잡아 두는 이유는, 나중에 값을 넣을 때 어느 코드였는지 다시
  //   찾지 않게 하기 위해서다. (dp-mapping §4-2 · dp-lint 「남은 문제」 1)
  phoneLocks: {
    'GYQV-38': '0000', // A6 · 서장현의 휴대폰
    'DMYX-34': '0000', // C6 · 오정숙의 폰
    'LIPT-58': '0000', // D6 · 흐엉의 폰
    'TQMO-65': '0000', // E6 · 윤도경의 휴대폰   (lock.hint: '시작한 날')
    'KZBP-76': '0000', // X10 · 박태식 폰
    // B5 차민우 폰(TLBI-94)과 F5 임기석 폰(DMKO-85)은 잠겨 있지 않다 — 자리도 두지 않는다.
  },

  // ── 카톡 톡서랍 복구 비번 ──────────────────────────────────────────────────
  // 야간조에는 없다. 「지워진 대화를 복구한다」가 이 판의 장치가 아니다
  // (A6 은 사진 휴지통, C6·E6 은 지우지 않은 대화 그대로가 요점이다).
  // 키를 지우지 않고 빈 객체로 두는 것은 mergeSecrets 의 구조분해 기본값과 모양을
  // 맞추기 위해서다 — 나중에 한 줄 더할 자리가 눈에 보인다.
  recover: {},

  // ── 태블릿 관리자 조회 하나(T1) ────────────────────────────────────────────
  // browser 앱의 lookup 에 { answer, result } 를 덮어쓴다. answer 는 관리자 사번,
  // result 는 조회가 성공했을 때 뜨는 화면이다(제목 한 줄 + 줄 목록).
  // 데이터 쪽(ZVLJ-37)에는 site·label·placeholder·desc·notFound 까지만 있다.
  lookups: {
    'ZVLJ-37': {
      answer: 'CHANGE-ME',
      result: {
        title: '(예시) 조회 결과',
        lines: [
          '이 값은 secrets.yaganjo.js 에서 실제 판정 내용으로 교체하세요.',
          '전표의 상태 칸과 조회 로그가 여기에 줄 단위로 들어갑니다.',
        ],
      },
    },
  },
};
